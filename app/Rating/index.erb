<div class="pageTitle">
  <h1>Ratings</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @ratings.each do |rating| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => rating.object %>">
            <span class="title"><%= rating.rate %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>