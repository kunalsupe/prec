<div class="pageTitle">
  <h1>Payments</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @payments.each do |payment| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => payment.object %>">
            <span class="title"><%= payment.plan %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>